# How it works:
Min_value and max_value is calculated based on players location and item consumed.
RNG is generated in range of min_value and max_value.
The longer player walks, the higher chance for enemy to spawn.
